import * as React from 'react';

import { I{{properCase name}}Props } from '.';

/**
 * {{properCase name}} component.
 * @param props - {{properCase name}} props.
 * @returns Header with component name.
 */
export const {{properCase name}}: React.FC<I{{properCase name}}Props> = (props) => {
    const {} = props;
    return (
        <div>
            hello {{properCase name}}
        </div>
    );
};
